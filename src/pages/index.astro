---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import MainNav from '../components/MainNav.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
---

<!DOCTYPE html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<main>
			<div class="infos">
				<div class="title">
					这里是日摇圈核狗程序员 <b><span class="brand">DeAnti</span> 的 Blog</b>；
          偶尔搞开发，偶尔弹贝斯，偶尔看live。
				</div>
				<div class="desc">
					Welcome to the official <a href="https://astro.build/">Astro</a> blog starter template. This
					template serves as a lightweight, minimally-styled starting point for anyone looking to build
					a personal website, blog, or portfolio with Astro.
				</div>
        <nav>
          <a href="/" class="active"eHome</a>
          <a href="/list">List</a>
          <a href="/about">About</a>
        </nav>
			</div>
			<div class="images">
			</div>
		</main>
		<Footer />
	</body>
</html>

<style lang="less">
	html {
		height: 100%;
	}
	body {
		display: flex;
		flex-direction: column;
		align-items: center;
		height: 100%;
	}
	main {
		display: flex;
		flex-direction: row;
		align-items: center;
		width: 100%;
		height: 100%;
		padding: 120px;
	}
	.infos {
		flex: 3;
		padding: 40px 40px 160px;
    	text-wrap: balance;
    	word-break: normal;
	}
	.title {
    	font-size: 48px;
		b {
			font-family: "Inter", "SourceHanSans CN";
		}
		.brand {
		}
	}
  .desc {
    margin-top: 28px;
    font-size: 17px;
    font-weight: 500;
    line-height: 1.6;
  }
  nav {
    font-family: "Fira Code";
    display: flex;
    flex-direction: row;
    margin-top: 20px;
    font-weight: bold;
		a {
			position: relative;
			list-style: none;
			padding: 0 20px;
			cursor: pointer;
			transition: color .3s ease-out;
			&:first-child {
				padding-left: 0;
			}
			&:not(:first-child):before {
				content: '|';
				position: absolute;
				left: 0;
				transform: translateX(-50%);
				color: var(--FG-0);
			}
			&:hover {
				color: var(--FG-3);
				&:not(:first-child):before {
					color: var(--FG-0);
				}
			}
			&.active {
				color: var(--FG-1);
			}
		}
  }
  .images {
	flex: 4;
  }
</style>
