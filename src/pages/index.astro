---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION, MAIN_LANG_CODE } from '../consts';
---

<!DOCTYPE html>
<html lang={MAIN_LANG_CODE}>
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
		<style lang="less">
			html {
				height: 100%;
			}
			body {
				display: flex;
				flex-direction: column;
				align-items: center;
				height: 100%;
			}
			main {
				display: flex;
				flex-direction: row;
				justify-content: space-around;
				align-items: center;
				width: 100%;
				height: 100%;
				padding: 120px;
			}
			.infos {
				flex: 3;
				padding: 40px 40px 160px;
				text-wrap: balance;
				word-break: normal;
			}
			.title {
				font-size: 48px;
				b { font-family: "Inter", "SourceHanSans CN"; }
			}
			.brand {
				position: relative;
				background: linear-gradient(-30deg, rgb(39, 122, 218), rgb(74, 230, 121), rgb(201, 214, 13), rgb(226, 20, 233), rgb(16, 172, 219), rgb(39, 122, 218), rgb(74, 230, 121), rgb(201, 214, 13), rgb(226, 20, 233), rgb(16, 172, 219));
				background-size: 1000%;
				background-clip: text;
				-webkit-background-clip: text;
				-webkit-text-fill-color: transparent;
				animation: neonBrand 5s linear reverse infinite;
				z-index: 1;
			}

			@keyframes neonBrand {
				0% { background-position: 0%; }
				100% { background-position: 66%; }
			}
			.desc {
				margin-top: 28px;
				font-family: 'Fira Code';
				font-size: 17px;
				font-weight: 500;
				line-height: 1.6;
			}
			nav {
				font-family: "Fira Code";
				display: flex;
				flex-direction: row;
				margin-top: 20px;
				font-weight: bold;
				a {
					position: relative;
					list-style: none;
					padding: 0 20px;
					cursor: pointer;
					transition: color .3s ease-out;
					color: var(--FG-0);
					text-decoration: none;
					&:first-child {
						padding-left: 0;
					}
					&:not(:first-child):before {
						content: '|';
						position: absolute;
						left: 0;
						transform: translateX(-50%);
						color: var(--FG-0);
					}
					&:hover {
						color: var(--FG-3);
						&:not(:first-child):before {
							color: var(--FG-0);
						}
					}
					&.active {
						color: var(--FG-2);
					}
				}
			}
			.images {
				flex: 4;
				display: grid;
				// grid-template: "a a b b" 1fr
				// 							"a a b b" 1fr
				// 							"c c b b" 1fr/
				// 							1fr 1fr 1fr 1fr;
				grid-template-areas: "a b" "a b" "c b";
				img:nth-child(1) {
					grid-area: a;
				}
				img:nth-child(2) {
					grid-area: b;
				}
				img:nth-child(3) {
					grid-area: c;
				}
			}
		</style>
	</head>
	<body>
		<main>
			<div class="infos">
				<div class="title">
					这里是日摇圈核狗程序员 <b><span class="brand">DeAnti</span> 的 Blog</b>；
          偶尔搞开发，偶尔弹贝斯，偶尔看live。
				</div>
				<div class="desc" lang="en">
					Welcome to DeAnti's personal blog. Here you will find development, music, and photography pieces written by a J-Rock Harcode guy.
				</div>
        <nav>
          <a href="/" class="active">Home</a>
          <a href="/blog">List</a>
		  		<a href="/tags">Tags</a>
          <a href="/about">About</a>
        </nav>
			</div>
			<div class="images">
				<img src=""/>
			</div>
		</main>
		<Footer />
	</body>
</html>
